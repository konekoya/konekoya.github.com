(function($) {

  // no need to include document.ready function here,
  // since we included the script before closing body tag
  $('#fullpage').fullpage({
    css3: true,
    fitToSection: true
  });

}(jQuery));
